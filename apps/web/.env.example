# Leave empty during local development to use the Vite proxy.
# Set this in Netlify to the deployed Cloudflare Worker origin, without a trailing slash.
VITE_API_URL=https://ai-search-api.your-subdomain.workers.dev
